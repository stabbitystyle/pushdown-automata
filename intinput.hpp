// Int Input class
// Rev0 7/4/18 by William Frank

#ifndef INTINPUT_HPP
#define INTINPUT_HPP

using namespace std;

// intInput is a helper function which parses input for an int and returns a boolean value depending on whether it was a valid non-negative integer
// the result is then passed through the argument out of the function as a side effect
bool intInput(int& userInput);

#endif